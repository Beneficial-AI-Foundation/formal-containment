#+title: Artifacts

File ~Basic.lean~ is a stub that gets overwritten in teach tmpdir copy of the ~imp~ package.

Files ~{Positive,Negative}.lean.template~ is the template out of which ~Basic.lean~ is formed.
