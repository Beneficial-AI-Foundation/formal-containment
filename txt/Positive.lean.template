import Aesop
import Imp
open Imp

example : {% if metavariables %}forall ({{ metavariables }} : Int), {% endif %}
{{ "{{astn " + specification.precondition + "}}" }}({{ command }}){{ "{{astn " + specification.postcondition + "}}" }} := by
  {{ proof }}
